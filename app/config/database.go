package config

/*
|--------------------------------------------------
| Default Application Database Connection Type
|--------------------------------------------------
|
| This defines what the default database for the
| project should be. MySQL is the default, but it
| also supports PostgreSQL and SQLite.
|
*/

var Database = "MySQL"
